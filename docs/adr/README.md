# Architecture decision records

Use an ADR for choices that constrain future implementation or are costly to reverse. Name files `NNNN-short-name.md`, starting at `0001`.

Accepted ADRs are immutable. If a decision changes, add a new ADR that supersedes the old one.
